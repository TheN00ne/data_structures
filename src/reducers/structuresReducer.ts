import { createSlice } from "@reduxjs/toolkit";

const structuresReducer = createSlice({
  name: "structures",
  initialState: {},
  reducers: {},
});

export default structuresReducer.reducer;
export const {} = structuresReducer.actions;
