import { createSlice } from "@reduxjs/toolkit";

const uiReducer = createSlice({
  name: "UI",
  initialState: {},
  reducers: {},
});

export default uiReducer.reducer;
export const {} = uiReducer.actions;
